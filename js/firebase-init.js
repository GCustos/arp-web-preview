/*
  ARP Prevención — Web pública
  Proyecto Firebase SEPARADO de "arp-inspecciones" (no mezclar).
  Rellena estos valores cuando crees el proyecto nuevo en la consola de Firebase.
  Recuerda: Firebase v8 compat, mismo patrón que la PWA.
*/
const firebaseConfig = {
  apiKey: "PENDIENTE",
  authDomain: "PENDIENTE.firebaseapp.com",
  projectId: "PENDIENTE",
  storageBucket: "PENDIENTE.firebasestorage.app",
  messagingSenderId: "PENDIENTE",
  appId: "PENDIENTE"
};

firebase.initializeApp(firebaseConfig);
const db = firebase.firestore();
